# Ensure pnpm only manages the root package.json and workspace packages
# This prevents accidental installation in internal package.json files
ignore-workspace-root-check=false
recursive-install=false
enable-pre-post-scripts=false
ignore-dep-scripts=true

# Completely ignore ai2svelte and legacy directories
ignore-patterns=ai2svelte/** legacy/**
